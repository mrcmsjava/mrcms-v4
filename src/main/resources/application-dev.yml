spring:
  devtools:
    restart:
      poll-interval: 2s
      quiet-period: 1s
      enabled: false
      additional-paths: src/main/resources/templates
    livereload:
      enabled: true
      port: 35729

logging:
  level:
    root: debug